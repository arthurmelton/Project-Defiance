using UnityEngine;

namespace sounds
{
    public class adioManiger : MonoBehaviour
    {
        public sound[] sounds;

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
