using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class zombiesLogo : MonoBehaviour
{
    public Sprite Sprite;

    // Start is called before the first frame update

    // Update is called once per frame
}
